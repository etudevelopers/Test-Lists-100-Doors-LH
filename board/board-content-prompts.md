# Session Prompts

A record of the prompts used to build this board, in order.

1. Create a new repository like etudevelopers/Test-Lists-LH for another learning hour: same "Test Lists" topic,
   exercise is the "100 doors" kata instead. Follow the same structure and create a new Miro board
   ("Test Lists - 100 doors - LH") in the same space as the previous learning hour board
   (https://miro.com/app/board/uXjVHVVo_9g=/), matching the style of
   https://miro.com/app/board/uXjVH07M1TQ=/. The team will use this board and repo for the next two weeks: week 1
   produces the test list, week 2 implements it.

2. Repo name confirmed as `Test-Lists-100-Doors-LH`.

3. Execute: scaffold the repo from ibanFR/learning-hour-template (matching how Test-Lists-LH was built), write the
   two-session agenda, then build the Miro board with the same frame structure and style constants as the
   reference template board (`uXjVH07M1TQ=`): background `#fbf7ef` (slides) / `#ffffff` (pair frames), text
   `#090909`, headings bold, body regular, `light_yellow` take-away stickies. Note: the reference board's
   `roobert` heading font isn't in the available Canvas Composer font list — substituted `roboto` (closest
   available geometric sans) for headings; body stayed `noto_sans`, matching the reference exactly.
